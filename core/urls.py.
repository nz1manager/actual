from django.urls import path
from django.http import JsonResponse

def simple_check(request):
    return JsonResponse({"status": "Success", "message": "Django is alive!"})

urlpatterns = [
    path('', simple_check),
]
