from django.contrib import admin
from django.urls import path
from django.http import JsonResponse

def home(request):
    return JsonResponse({"message": "Django Backend is Live!", "status": "success"})

urlpatterns = [
    path('admin/', admin.site.path),
    path('', home), # Asosiy sahifa tekshirish uchun
]
